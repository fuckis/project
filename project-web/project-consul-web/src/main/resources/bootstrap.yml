spring:
  application:
    name: consul-web

server:
  port: 8080

ribbon:
  connectTimeout: 2000
  readTimeout: 5000
  maxAutoRetries: 1
  maxAutoRetriesNextServer: 3
  okToRetryOnAllOperations: true
