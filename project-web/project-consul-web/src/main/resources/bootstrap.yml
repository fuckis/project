spring:
  application:
    name: consul-web

server:
  port: 8080

consul-service:
  ribbon:
    ServerListRefreshInterval: 2000
    ConnectTimeout: 3000
    ReadTimeout: 3000
    MaxAutoRetries: 1
    MaxAutoRetriesNextServer: 3
    OkToRetryOnAllOperations: true
